[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "claude-memory"
version = "0.1.0"
description = "Persistent memory with scope hierarchy for AI agents in Claude Code"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "Max Nardit", email = "max@nardit.com" }]
keywords = ["claude", "ai", "memory", "mcp", "agents"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Libraries",
]
dependencies = [
    "mcp>=1.0",
    "pyyaml>=6.0",
    "click>=8.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov"]

[project.scripts]
claude-memory = "claude_memory.cli:main"
claude-memory-session-start = "claude_memory.hooks:main_session_start"
claude-memory-post-tool-use = "claude_memory.hooks:main_post_tool_use"

[project.urls]
Homepage = "https://github.com/mnardit/claude-memory"
Repository = "https://github.com/mnardit/claude-memory"

[tool.pytest.ini_options]
testpaths = ["tests"]
